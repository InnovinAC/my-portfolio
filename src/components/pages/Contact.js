import Card from "../Card";
import Layout from "../Layout";
import NavbarMain from "../includes/Navbar";

export default function Contact() {
    return (
        <Layout>
        <Card header_color="success" header="Contact Me">
            <p> Use the form below to contact us</p>
        </Card>
        </Layout>
        
    )
}