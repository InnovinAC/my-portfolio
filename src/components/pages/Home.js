import Card from "../Card";
import Layout from "../Layout";
import NavbarMain from "../includes/Navbar";

export default function Home() {
    return (
        <Layout>
        <Card header_color="secondary" header="Home">
            <p> This is the home page</p>
        </Card>
        </Layout>
        
    )
}